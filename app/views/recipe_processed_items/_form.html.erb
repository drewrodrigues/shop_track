<%= form_for [@recipe, @recipe_processed_item] do |form| %>
  <% if recipe_processed_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recipe_processed_item.errors.count, "error") %> prohibited this recipe_processed_item from being saved:</h2>

      <ul>
        <% recipe_processed_item.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :recipe_id, value: @recipe.id %>

  <div class="field">
    <%= form.label :processed_item_id %>
    <%= form.select :processed_item_id, options_from_collection_for_select(ProcessedItem.all, :id, :name), required: true, prompt: 'Select an item' %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.text_field :quantity, required: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
