<%= form_with(model: combined, local: true) do |form| %>
  <% if combined.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(combined.errors.count, "error") %> prohibited this combined from being saved:</h2>

      <ul>
        <% combined.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
